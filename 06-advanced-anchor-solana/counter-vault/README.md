# Track C — Counter Vault

Standalone SPL vault with deposit counter (user CPI + PDA-signed withdraw).

| Folder | Purpose |
|--------|---------|
| [`starting_code/`](starting_code/) | Student skeleton — paste into Playground and complete the `TODO` items |
| [`final_solution/`](final_solution/) | Reference implementation + `architecture-notes.md` |

**No Part 05 prerequisite.** Add `anchor-spl` to `Cargo.toml` in Playground.
